(function() {
    define(['app'], function(app) {
        app.constant('backend', {
            // @if ENV == 'dev'
            // @endif
            // @if ENV == 'prod'
            // @endif
        });
    });
})();
